<Window x:Class="taogiaodien1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:taogiaodien1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="NHẬP THÔNG SỐ MÓNG ĐƠN" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" Foreground="DeepSkyBlue"/>

            <Label Content="Tải trọng tính toán N (kN):"/>
            <TextBox x:Name="txtN" Margin="0,0,0,10" Height="25"/>

            <Label Content="Mô men M (kNm) - Đúng tâm nhập 0:"/>
            <TextBox x:Name="txtM" Margin="0,0,0,10" Height="25" Text="0"/>

            <Label Content="Cường độ đất nền R (kPa):"/>
            <TextBox x:Name="txtR" Margin="0,0,0,10" Height="25"/>

            <Label Content="Độ sâu chôn móng h (m):"/>
            <TextBox x:Name="txth" Margin="0,0,0,10" Height="25"/>

            <Button x:Name="btnTinh" Content="TÍNH TOÁN KÍCH THƯỚC" Height="40" Margin="0,20,0,0" 
                Background="DeepSkyBlue" Foreground="White" FontWeight="Bold" Click="btnTinh_Click"/>
        </StackPanel>

        <Border Grid.Column="1" Background="#F0F0F0" CornerRadius="10" Padding="15">
            <StackPanel>
                <TextBlock Text="KẾT QUẢ" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <TextBlock Text="Diện tích yêu cầu (Fp):" Margin="0,5"/>
                <TextBlock x:Name="lblFp" Text="0.00 m2" FontWeight="Bold" Foreground="Red" Margin="0,0,0,10"/>

                <TextBlock Text="Kích thước cạnh móng (a):" Margin="0,5"/>
                <TextBlock x:Name="lblCanh" Text="0.00 m" FontWeight="Bold" Foreground="Red"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
